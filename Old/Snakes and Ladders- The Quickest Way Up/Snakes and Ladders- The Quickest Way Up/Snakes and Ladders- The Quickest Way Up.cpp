// Snakes and Ladders- The Quickest Way Up.cpp : Defines the entry point for the console application.
// url : https://www.hackerrank.com/challenges/the-quickest-way-up

#include <stdio.h>
#include <iostream>
#include <cmath>
#include <algorithm>
#include <functional>
#include <vector>

using namespace std;

int main()
{
	int T, i, j, lad, sna;
	scanf("%d", &T);
	while (T--)
	{
		vector<int> grid(107, 0);
		scanf("%d", &lad);
		vector<int> ladders(lad);
	}










	return 0;
}

